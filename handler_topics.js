/**
 * ═══════════════════════════════════════════════════════════════
 *  handler_topics.js — نظام التحقق الجامعي المتطور v4.0
 *
 *  📂 هذا الملف يجمع الوحدات الفرعية:
 *  ─────────────────────────────────────────────────────────────
 *  • verify_helpers.js      — ثوابت + دوال مساعدة + خطوات التسجيل
 *  • verify_registration.js — اعتراض الأعضاء الجدد + أزرار التسجيل
 *  • verify_actions.js      — قبول/رفض/حظر + فلتر + فحص دوري
 *  • verify_commands.js     — أوامر الطلاب والإدارة
 *
 *  🎓 ميزات نظام التحقق:
 *  ─────────────────────────────────────────────────────────────
 *  ✅ عضو جديد ينضم → يُقيَّد فوراً في كل المواضيع
 *  ✅ يظهر له موضوع واحد فقط (الخاص مع البوت)
 *  ✅ يُطلب منه: رقم القيد → الكلية → التخصص → السنة (بالأزرار)
 *  ✅ الطلب يُرسل للمالك + المشرفين + المطور مع أزرار قبول/رفض
 *  ✅ عند القبول → يُفك تقييده ويُفتح له موضوع كليته فقط
 *  ✅ فحص دوري كل 30 دقيقة للأعضاء الموجودين غير المعتمدين
 *  ✅ منع التسجيل في أكثر من كلية واحدة
 *  ✅ Cooldown 24 ساعة بعد الرفض
 *  ✅ المواضيع تُجلب تلقائياً من المجموعة عبر /synctopics
 *
 *  📋 قائمة الأوامر:
 *  ─────────────────────────────────────────────────────────────
 *  للطلاب:
 *    /verify          — بدء التسجيل (في الخاص)
 *    /mytopics        — حالة الاعتماد
 *
 *  للمشرفين:
 *    /verify_system on|off  — تفعيل/تعطيل النظام
 *    /verify_requests       — عرض الطلبات المعلقة
 *    /verify_stats          — إحصائيات النظام
 *    /revoke_verify         — إلغاء اعتماد طالب
 *    /synctopics            — مزامنة المواضيع تلقائياً
 *    /regtopic              — تسجيل موضوع يدوياً
 *    /check_unverified      — فحص الأعضاء غير المعتمدين
 * ═══════════════════════════════════════════════════════════════
 */

const setupVerifyRegistration = require('./verify_registration');
const setupVerifyActions      = require('./verify_actions');
const setupVerifyCommands     = require('./verify_commands');

module.exports = function setupTopicHandlers(bot) {
  // ترتيب التسجيل مهم:
  // 1. التسجيل أولاً (اعتراض الأعضاء + أزرار)
  setupVerifyRegistration(bot);

  // 2. الإجراءات (قبول/رفض + فلتر + فحص دوري)
  setupVerifyActions(bot);

  // 3. الأوامر (synctopics, verify, verify_system, إلخ)
  setupVerifyCommands(bot);
};
